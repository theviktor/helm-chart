apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana
{{ include "common.labels" . | indent 4 }}
  name: grafana
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: grafana
      port: 3000
      targetPort: 3000
  type: NodePort
  selector:
    app: grafana
